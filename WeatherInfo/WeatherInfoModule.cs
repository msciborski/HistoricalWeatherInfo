using Autofac;

namespace WeatherInfo
{
    public class WeatherInfoModule : Module
    {
        protected override void Load(ContainerBuilder builder)
        {
            base.Load(builder);
        }
    }
}